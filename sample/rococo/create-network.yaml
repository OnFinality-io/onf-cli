name: rococo-local
displayName: Rococo created by cli
protocol: substrate
imageRepository: parity/polkadot
imageVersion: v0.9.7
config:
  nodeTypes:
    full:
      args:
        - key: --chain
          value: "rococo-local"
      env:
        - key: TEST
          value: "TEST"
    validator:
      args:
        - key: --chain
          value: "rococo-local"
